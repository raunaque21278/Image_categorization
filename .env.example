# Docker environment — copy to .env in project root:
#   cp .env.docker.example .env

JWT_SECRET=
HF_API_KEY=
GOOGLE_VISION_KEY_PATH=./worker/config/google-vision.json
CORS_ORIGIN=http://localhost:3000
